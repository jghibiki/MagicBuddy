'use strict';

describe('magicBuddy.deck module', function() {

  beforeEach(module('magicBuddy.deck'));

  describe('deck controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var deckCtrl = $controller('DeckCtrl');
      expect(deckCtrl).toBeDefined();
    }));

  });
});
