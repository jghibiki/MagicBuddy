'use strict';

describe('magicBuddy.collection module', function() {

  beforeEach(module('magicBuddy.collection'));

  describe('collection controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var collectionCtrl = $controller('CollectionCtrl');
      expect(collectionCtrl).toBeDefined();
    }));

  });
});
