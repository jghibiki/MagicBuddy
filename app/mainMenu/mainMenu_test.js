'use strict';

describe('magicBuddy.mainMenu module', function() {

  beforeEach(module('magicBuddy.mainMenu'));

  describe('mainMenu controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mainMenuCtrl = $controller('MainMenuCtrl');
      expect(mainMenuCtrl).toBeDefined();
    }));

  });
});
